import { BaseComponent } from '../../components/base-component';

export class Winners extends BaseComponent {
  constructor() {
    super({
      tagName: 'main',
      classNames: 'winners',
    });
  }
}
